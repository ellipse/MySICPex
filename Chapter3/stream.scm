;; primitives for constructing and operating on streams

(define (memo-poc) )
(define (delay) )
(define (force) )
(define (cons-stream) )
(define (stream-car) )
(define (stream-cdr) )
(define (add-stream) )
(define (map-stream) )
